<%= form_with(model: gretting) do |form| %>
  <% if gretting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gretting.errors.count, "error") %> prohibited this gretting from being saved:</h2>

      <ul>
        <% gretting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
